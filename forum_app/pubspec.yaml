name: forum_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  provider: 
  supabase_flutter: 
  go_router:

  image_picker:
  flutter_skill: ^0.9.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  mocktail:

flutter:
  uses-material-design: true
